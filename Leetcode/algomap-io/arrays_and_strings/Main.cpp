#include <iostream>
#include <vector>

#include "Closest_to_zero"

using namespace std;

int main()
{
    vector<int> nums = { 10, -4, -2, 18451, 4, 8 };

    cout << findClosestNumber(nums);

    return 0;
}
